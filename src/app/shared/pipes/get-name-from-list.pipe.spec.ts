import { GetNameFromListPipe } from './get-name-from-list.pipe';

describe('GetNameFromListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNameFromListPipe();
    expect(pipe).toBeTruthy();
  });
});
